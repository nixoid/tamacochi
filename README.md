# Tamacochi

A pocket-sized pixel pig pet for mobile browsers. Feed or play with Pip, then watch the pet wander through its toy-screen meadow.

## Development

```sh
npm install
npm run dev
```

## Production checks

```sh
npm run lint
npm run build
```

The app is installable as a PWA and caches its application shell for offline use after the first visit.
